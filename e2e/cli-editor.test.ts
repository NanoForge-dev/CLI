// ! Removed until editor is released
// import { describe, expect, it } from "vitest";
//
// import { runCli } from "./helpers/run-cli";
//
// describe("nf editor (keepAlive behavior)", () => {
//   it("should keep the process alive", async () => {
//     const { killed } = await runCli(["editor"], { timeout: 3000 });
//
//     expect(killed).toBe(true);
//   });
//
//   it("should keep the process alive with a path argument", async () => {
//     const { killed } = await runCli(["editor", "/tmp", "--no-open"], { timeout: 3000 });
//
//     expect(killed).toBe(true);
//   });
// });
